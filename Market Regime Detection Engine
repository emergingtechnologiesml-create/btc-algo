print(" Regime distribution:")
for rg, pct in out['market_regime'].value_counts(normalize=True).items():
print(f" {rg}: {pct:.2%}")


for regime, sub in df.groupby('market_regime'):
axes[0, 1].scatter(sub.index, sub['close'], s=2, c=regime_colors.get(regime, 'gray'), alpha=0.6, label=regime)
